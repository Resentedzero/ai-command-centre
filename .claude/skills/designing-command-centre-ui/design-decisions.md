# Design decisions and proposals

Visual decisions are recorded here with their status. Product or API proposals raised by design work are also recorded here. **A proposal authorizes nothing.** Backend work follows the spec's own process.

## Decisions

| # | Decision | Status |
|---|---|---|
| D1 | Art direction: a dungeon-fantasy world that has evolved into sci-fi mission control ("the dungeon is the computer"). The Tile Pack is the core world asset library, and the world view shows agent activity. This supersedes the "sci-fi ops center" wording in spec §15.4 and closes TILE_PACK_INVENTORY's open next step 2. | **Decided** by the operator on 2026-09-14. Recorded in a §15.4 implementation note. **The sci-fi part is superseded by D19:** the chrome is pure pixel and sci-fi is optional. "The dungeon is the computer" stands. |
| D2 | Reference images stay untracked in `assets/References for Ui/`, described in `references.md`. | **Decided** by the operator on 2026-09-14 |
| D3 | The MVP's "plain inline styles, no design system" ruling (Unit 11, Ruling 4) ends when UI work begins. Its replacement: CSS custom properties plus CSS Modules, with no Tailwind or component library. Adding one needs a recorded reason. | Proposed |
| D4 | Fonts: Pixelify Sans (world and titles), IBM Plex Sans (HUD), JetBrains Mono (telemetry), via `next/font`. | **Decided** by the operator on 2026-09-14. **Amended by D19:** IBM Plex Sans is no longer used. Pixelify Sans is for titles, labels and buttons; JetBrains Mono is for API values and long reading. |
| D5 | Figma is the visual source of truth: [AI Command Centre UI](https://www.figma.com/design/3Q5IMyXoPHDBFDv8vjS467/AI-Command-Centre-UI), file key `3Q5IMyXoPHDBFDv8vjS467`. | **Decided** by the operator on 2026-09-14 |
| D6 | Station per Invocation kind: Alchemy = `llm`, Anvil/Workbench = `tool`, Furnace = `deterministic`. | Proposed (presentation only) |
| D7 | Agent identity tints avoid state hues. Status is always an icon and a label, never colour alone. | Decided (accessibility) |
| D8 | World rendering: CSS sprite strips and DOM rooms first. Move to `<canvas>` only if many concurrent animated sprites measurably stutter. | Proposed |
| D9 | World pixel scale per screen. Figma can't rescale pixel art without blurring it, so each scale is its own upload set. At 4× (tile = 192 px) an Overview world fits under 7 tiles across a 1920 px frame. Proposal: **4×** for component docs and the Agents screen's single large room; **2×** for the Overview and Workflows worlds, as a second cut-and-upload pass from `tile-slices.json`. | **Decided** 2026-09-14 (the operator delegated it): 2× for map screens, 4× for close-ups and component docs |
| D10 | Overview mockup rooms at 2× are frames built from the 2× source art, not `Room` instances, because `Room` is 4× only. In code there is one React `Room` with a `scale` prop (2 or 4) and the same `state` variants, so there is no second component. Add a `scale` variant in Figma only if a second map screen needs it. | **Decided** 2026-09-14 (delegated) |
| D12 | "More rooms" on Overview are **system rooms**, never placeholder agents. Goals, Workflows, Approvals, Events and Artifacts each sit on a main corridor, show a real value (or "gated"), and open their screen. The agent wing beside the hub has one room per `GET /agents/active` row and grows with the data. | **Decided** 2026-09-14 (operator asked for more rooms; mapping delegated) |
| D13 | Extra art packs, both confirmed usable in the app by the operator on 2026-09-14:<br>• *Top Down Adventure Pack v1.0* (o_lobster; credit o_lobster)<br>• *Pixel 16 Interiors v2 free*<br>Both are in `assets/gamification/Tile Pack/`. Only props are taken from them. Their walls and floors are excluded because the purple-grey palette clashes with Pixel Crawler teal. Status-coloured or misleading art is excluded: the blue crystal, spikes, enemies, health HUD, outdoor sheets and home furniture. | **Decided** 2026-09-14 (licence by operator; selection delegated) |
| D14 | *Cute_Fantasy_Free* (in `assets/gamification/Tile Pack/`) **may be used**. Its `read_me.txt` licence is non-commercial only, with no redistribution even modified. The operator accepted that on 2026-09-14 ("its fine to use"). **Keep this pack out of any public repo or public bundle, and re-check before any commercial use.** Its style is brighter and outdoor, so it is used for outdoor areas around the dungeon, not inside rooms. | **Decided** by the operator 2026-09-14 |
| D16 | The Overview world is a **scrollable map** larger than the viewport. The operator pans by drag or scroll, and the pan position is client state. A minimap in the world corner shows the whole map and the current view rectangle, and only navigates rooms that exist. The map is 2× pixel scale throughout. | **Decided** by the operator 2026-09-14 |
| D15 | 16 more Pixel Crawler decorations from `Environment/Props/Static` (Esoteric, Dungeon_Props, Furniture), cut by connected-pixel bounds. They are listed in `tile-slices.json` → `extraSlices`, are ambience only and carry no data. | **Decided** 2026-09-14 (operator: "add more if you can") |
| D17 | **Buildings outside the keep** map to real concepts or are unlabelled scenery:<br>• **Project houses:** a village on the west road, one house per `ProjectGoals` row from `GET /goals`. The plate shows `name` and the goal count (capped list, so 500+).<br>• **Registry armory** and **Cost treasury** on the east road, as gated entries to those screens. They show "gated" and no values until their read models exist.<br>• A few unlabelled cottages as ambience only. Never label a building with a feature that doesn't exist. | **Decided** 2026-09-14 (operator asked for houses "for other stuff"; mapping delegated) |
| D18 | **Any art pack in `assets/` may be used, whatever its licence says.** This supersedes the licence cautions in D13/D14 and covers the new *Modern tiles_Free* pack (no licence file). Licence compliance is the operator's responsibility. Practical guard kept: don't publish third-party pack files to a public repo or bundle without re-checking. | **Decided** by the operator 2026-09-14 |
| D19 | **Visual direction: pure pixel keep ("the dungeon/village is the computer").** Adopted after a three-way comparison (cycle 2, `design-reviews.md`).<br>• **Sci-fi is not used by default:** no cyan core, vector conduits, holo brackets or Plex uppercase chrome.<br>• **Chrome is pixel:** bevel frames (2 px outline plus a 2 px edge), dark wood bars and boards, parchment cards, Pixelify Sans for titles, nav, tags and labels.<br>• **JetBrains Mono** stays for raw state tokens, ids, times and long reading text (legibility).<br>• **New Figma colour variables `pixel/*`:** wood-dark, wood, wood-edge, outline, parchment, parchment-shade, ink, label, label-dim.<br>• **State hues are unchanged and reserved for state.** Cyan = active; amber = awaiting approval / pending approval / paused; red = failed or stop; green = done or live.<br>• **The runtime core is pale silver**, and ambient room light is dim neutral warm grey.<br>• **Light is dithered pixel light** clipped to room floors (`assets/gamification/adapted/make-light-pools.ps1`).<br>• **The world is one continuous keep of varied rooms** (`compose-w2-keep.ps1`). | **Decided** 2026-09-14 (operator delegated the choice to visual evidence; critic verdict) |
| D20 | **Agents beyond the keep's workshops.** The keep composite has two workshop rooms. When the API returns more active Agent Definitions than workshops:<br>• the extra agents are listed on the Overview board, each with its status mark, so no real activity is hidden;<br>• a wider keep (more workshop wings, composed by `compose-keep-v4.ps1`) is the design follow-up once more than two agents run in practice.<br>**Also:** D17's Registry armory and Cost treasury may now show real values, from `GET /registry` and `GET /costs` (routes added 2026-09-15). | **Decided** 2026-09-15 (design QA of the implementation, delegated) |
| D11 | Overview mockup text: agent names are the two real seeded agents. Every other dynamic value is written as its `api.ts` field path (`runs[0].goal.title`, `occurredAt`), so the frame never reads as real data. Counts in the top strip are example row counts. | **Decided** 2026-09-14 (delegated) |
| D21 | **Deliverables render as documents** (V1.1). `react-markdown` + `remark-gfm` are added to `web/`, the one library D3 asks a reason for: GFM tables, nested lists and code need a real parser, and a hand-written one is more code and more injection surface. They produce React elements only: `skipHtml`, no images, react-markdown's default URL transform (drops `javascript:`), external links `noopener noreferrer`. Document-like Artifacts (`deliverable`, `report`, `keeper_answer`) open on **Document** on dark vellum; **Evidence** (integrity with a re-verify, provenance, sources and the runtime-recorded evidence basis) and **Raw** (stored bytes) are secondary tabs. Other Artifacts open on Raw. Tab selection is a cream edge. | **Decided** by the operator 2026-09-15 (V1.1 brief §9) |
| D22 | **The Keeper** (V1.1). A guide, not an agent: it stands in the keep's **entrance hall** (the room that represented nothing) as the outlined **Rogue** NPC, with no state light and no workshop, and a "Keeper" plaque. A small **Keeper** door (bottom right, above the notice strip) opens the same panel from every screen. The panel is a wood board over the right edge: **This page / The keep** (deterministic explanation of the run, agent or artifact on screen), **Ask** (guide cards as Markdown), **Think** (explicit, labelled as using subscription quota; the answer renders as a Document; a proposal is a link to a pre-filled builder). The Rogue is a **single idle frame** (`sprites-outlined-2x/rogue-idle-2x-outlined.png`, copied by `copy-world-art.mjs`): an animated Rogue strip is owed by the design workstream (`make-outlined-strips.ps1`), and no asset under `assets/` was written. | **Decided** 2026-09-15 (V1.1 brief §11–12; art pending the design workstream) |
| D23 | **Builders and autonomous work** (V1.1). Agent Builder (`/agents/new`, `?from=` for a new version) and Workflow Builder (`/workflows/new`) are wood work-tables of fieldsets, one column at 960 px. The agent board gains **New version**, a versions line, a confirmed **Revoke** per key, the execution profile line and **Give an objective**. Objective steps get an **Autonomous work** panel above their invocations: one vellum row per iteration (runtime words: action, outcome, the model's short note, decision/result links), and a parchment status line that says why the loop stopped in plain words beside the runtime reason. No progress bars, no invented percent complete. | **Decided** 2026-09-15 (V1.1 brief §6–7, §13–14) |
| D24 | **Agent visual identity and recruitment** (R2). How an agent looks belongs to its persistent **name**, not a Definition version: `agent_appearances(agent_name, appearance)` holds catalogue keys only (`src/definitions/appearanceCatalogue.json`: skin, hair, hair colour, top, top colour, bottom, bottom colour, accessory, role mark). It is presentation only — no Grant, budget, routing, context or performance code reads it (structural invariant), and saving it writes no version and no event. Art is the **character kit**: Body_A idle (4) and run (6) with one layer strip per option (`make-agent-kit.ps1` → `agent-kit-2x/`, 182 strips at 68 px), stacked as one element's CSS backgrounds that step together. Cloth colours are the identity tints plus neutrals, never state hues. The kit has no death strip: a failed dressed agent holds its idle frame. The Agent Builder gains a **Character** station (workshop-floor preview standing and walking, captioned "preview · not a run"); a new version shows it read-only. **Change appearance** (`/agents/:id/appearance`) saves only the look. Agents with no look keep Knight/Wizzard, now by sorted name, so v1 and v2 match. Workshop slots also key on the name. No Figma frame was added this pass. | **Decided** 2026-09-16 (R2 brief: visual identity + recruitment; design delegated) |
| D25 | **Agent progression on the agent board** (R2). A **Progress** parchment under the role card, for the persistent agent (all versions): an `Lv N` plaque edged in identity violet, a 10-segment XP bar filled in identity violet (a level bar, not work progress; never a state hue), "N XP · M to level L+1", then plain lines for work (measured runs across all versions), speciality and reputation, each with an honest "not yet" state, achievement plaques, and a collapsed **How this XP was earned** ledger linking each award to its deliverable or workflow, with a note that levels grant no keys. The artifact screen gains **Your quality verdict** (poor/acceptable/good/excellent, optional reason, history), worded as the operator's judgement and explicitly not an approval. New token `--identity-violet`. No Figma frame this pass. | **Decided** 2026-09-16 (R2 progression brief; design delegated) |
| D26 | **Keeper explanations** (R2 Stage 6). The Keeper panel gains **Explain** at the top: wood choice plaques for exactly the intents the Keeper supports on this page's subject (from `GET /keeper/identity`), a question box, and an answer on vellum in three labelled parts — **Recorded** (each line with its source in dim mono and its record links), **Calculated from the records**, **Not known from the records** — ending "Read from the records without a model · about N tokens of context". An unsupported question lists what can be explained instead. "Look it up" becomes **How to** (guide cards for the same question); **Think** is worded as putting the same records into words, and shows an amber-edged warning when its answer used numbers its records did not contain. The agent Progress card gains ink "Why this level? / How was the XP earned? / Why these achievements? / Why this speciality?" links that open the Keeper on that question. The Keeper is drawn (dock, panel, entrance) through the appearance system, standing still, and stays the Rogue (D22) until it has an appearance. No Figma frame this pass. | **Decided** 2026-09-16 (R2 Stage 6 brief; design delegated) |
| D27 | **The living workplace** (plan §13; supersedes D12's and D20's "one room per active agent" and the fixed workshop slots, and the Keeper-in-the-entrance part of D22). Every persistent agent (one per name, its latest look) lives in the keep. What it is doing comes only from the runtime: an active run walks it along the halls to a workstation for that kind of work (research, analysis, writing, publishing, thinking, else generic), where it stands under the cyan active light while the run lasts; awaiting approval walks it to the council hall (the amber seal stays); a stop freezes it behind a red barrier; a queued run stands it in the plaza. With no unfinished run it lives ambiently in the common, rest and social areas (wandering, resting on the benches, gathering), with no name tag and an accessible label ending "(ambient, no work)"; real states carry a name tag naming the activity. Walking uses the existing down-facing run strips and CSS transitions between waypoints; resting holds the idle frame (no sit, side or up art exists — documented, not faked). Reduced motion removes the glide. Nav: tabs narrow to 100 px to add **History**; **Settings** sits beside the chips. History is a filter board plus vellum goal rows with Archive/Unarchive on finished goals; Settings → World is a mini-map with dashed area outlines (living areas violet), the building → area → workstation tree and a parchment editor. No Figma frame this pass. | **Decided** 2026-09-16 (living workplace brief; design delegated) |
| D28 | **Living world refinement** (plan §14; amends D24, D22/D26's Rogue fallback and D27's walking). **Every agent is drawn from the character kit.** An agent with no chosen appearance gets a look derived from a hash of its persistent name (`derivedAppearance`, API field `look`), never stored and replaced the moment a look is chosen; Knight/Wizzard/Rogue remain only as a fallback for an agent the Registry did not return. **Kit v2:** Body_A Idle (4), Walk (6) and Collect frames 0–5 as "work" (6; frames 6–7 raise the arms over the head and do not layer cleanly), each in Down, Side (faces right) and Up; left is Side mirrored with `scaleX(-1)`. Walking plays the walk strip facing the direction of each step; at a desk the agent faces the workstation's configured facing (default up) in the work pose; arrivals turn down or to a side; resting and gathering hold idle (no sitting art in this style — documented, not faked). Real work and approvals are walked to at twice the stroll so short runs are still seen at the desk. **Ambient spread:** idle agents choose among all living areas weighted by size and by how many others are there, pause in place half the time, and start at seeded, different times. **Catalogue v2:** 5 skins, 7 hair styles × 8 colours, 7 tops, 5 bottoms, 11 cloth colours, 6 accessories, 9 role marks and 8 presets (Archivist, Scout, Scribe, Artificer, Herald, Warden, Alchemist, Analyst) offered as preset buttons in the Character station. **World templates:** The Keep, Scholars' Keep, Garrison — applying one retires the current world (kept, not deleted). **Settings** has General (motion, ambient life, name tags), World, Agents (a new recruit's starting preset) and System (current-work window); General, Agents and System are saved per browser, World in the database. **History** gains a Workflow runs tab and a count-then-confirm bulk archive; Overview, Goals and Workflows show current work only (unfinished, or active in the window) with a line pointing to History. No Figma frame this pass. | **Decided** 2026-09-16 (living world refinement brief; design delegated) |
| D29 | **Character interaction and Talk** (R2). Clicking any character in the world (idle or busy) opens a small wood board attached to it, 300 px wide, drawn above the character (below it near the top of the map), moving with it, clamped inside the map and panned into view once on opening; it sits above every character and room plaque (plaques now also sit above characters). It shows the name, an `Lv N` plaque edged in identity violet, a 10-segment XP bar and XP, the speciality when established, one status line from the runtime only ("Idle · <area>", "Working · Thinking", "Awaiting approval", "Paused", "Stopped", "Queued to start" — ambient walking or resting is always "Idle"), the area when real work is under way, **Talk to <name>** and **View profile**. Talk opens a small message box with the line "Real work: uses <name>'s own keys and budget. It can answer and propose, not act."; while the run works it shows "<name> is working on it" with **Watch the run**; the reply appears as a short violet-edged quote with **View full work**, **Reply artifact** and **Talk again**; a failure says "The talk failed: <recorded reason>" with **Open the run**; an API refusal says "Not started: <reason>". A stopped, waiting, paused or working agent's Talk is disabled with the reason. Escape or × closes it. No modal, no transcript. No Figma frame this pass. | **Decided** 2026-09-16 (character interaction brief; design delegated) |
| D30 | **Command and the Manager** (R2 management layer). A ninth nav tab, **Command** (tabs narrow to 90 px), opens a two-column screen: a wood board with the plain statement of what the Manager is and is not, an **Objective** box with **Give to the Manager**, a refusal line ("Not started: <API reason>"), and the list of missions with their status marks; beside it the chosen mission: the objective as a 20 px heading with its status ("the Manager is planning", "agents are working", "waiting for your approval", "completed", "escalated to you", "failed", "stopped"), a parchment **Blockers** card (with **Review approvals** when an approval waits), **Plan** (numbered tasks: agent, brief, "done when"; or "Validation refused the plan; nothing was delegated."), **Work** (one vellum card per Workflow Run — Manager plan, Delegated work, Follow-up — with each step's agent, plan/task/review, status and output link), and **Result** (the review summary on a violet-edged quote, each deliverable "verified by code" or "not verified: …", the mission report link). A closing line says the Manager's words are not status. The Manager is an ordinary character in the world (derived look, same panel); Talk to the Manager starts a mission and the panel says "Mission started: Manager is planning the work" with **Follow it in Command**. The Keeper's Think section adds "The Keeper explains; it does not organise work … in Command." No Figma frame this pass. | **Decided** 2026-09-16 (Manager brief; design delegated) |
| D31 | **Workplace: Calendar, meetings in the world, workplace settings.** A tenth nav tab, **Calendar** (at 1440 px and below every tab sizes to its word at 14 px so ten tabs and the chips fit 1280). Calendar is a two-column screen: a wood board with Schedule/Notifications, Day/Week, ‹ Today ›, Agent and Room filters, "Meetings only", **New meeting**; a line stating the Keep's timezone, working hours and now; seven day columns whose meetings are parchment cards (ink text only; time, title, room · agents, status mark) and whose calendar entries are dashed wood cards; beside it the chosen meeting (when in the Keep's zone, room with seats and map area, organiser, participants, link to the Manager's mission, agenda, notes and decisions stamped with who recorded them, Move/Cancel for a scheduled meeting, Record the outcome once started, "Give to the Manager" per decision). In the world a real meeting is its own state: participants hurry to the room's map area and **stand** facing its middle (the kit has no sitting pose; nothing is faked), labelled "gathering for a meeting: …" / "in a meeting: …", never "working"; idle life does not wander into a room hosting a meeting; a room not on the map draws nobody inside it. The agent panel adds a compact **Today** schedule and **Next meeting**; its status tone for a meeting is neutral (cyan stays run work). Settings gains General → the Keep's clock (saved for everyone), Workplace (rooms), Agents → working hours, Notifications. No new art: rooms sit on drawn rooms that already have tables. No Figma frame this pass. | **Decided** 2026-09-17 (workplace brief; design delegated) |
| D32 | **Role icons: identity beside every name.** Each agent shows a 12x12 pixel symbol before its name (`[icon] Name`), from a governed catalogue of ten roles — command (crown), keeper (key), research (lens), field research (compass rose), ideas (lamp), analysis (bars), publishing (quill), review (shield), strategy (star compass), general (ring). Each has BOTH its own symbol and its own identity colour token (`--role-command` … `--role-generic`), so roles are told apart without colour; none of them is a `--state-*` hue, and runtime status keeps its own marks beside the name. Icons are decorative in the DOM (the name stays the accessible label; the tooltip names the role), 12 px by default and 14–18 px in headings, and appear in the world name tags, agent panel, Overview and agent rosters and profile, Calendar (participants, organiser, entries, notifications, chooser), Command (tasks, meeting participants, steps, evidence), History, workflow steps, Keeper explanations and Settings; `<option>` lists stay text-only. An agent the catalogue does not cover shows its name alone. A room hosting a real meeting also carries a wood marker naming the meeting and whether it is gathering or in progress. No new image assets: the icons are drawn as SVG pixels. No Figma frame this pass. | **Decided** 2026-09-17 (workplace + icons brief; design delegated) |

## Proposals raised by design (not authorized)

| # | Proposal | Why a screen wants it |
|---|---|---|
| P1 | `GET /agents`: every Agent Definition, not only active ones | An Agents roster that includes idle agents |
| P2 | Model id and provider on `InvocationDetail` | Inspector telemetry; the data exists in the `invocation_started` payload |
| P3 | Scope-level budget read model (day, goal) per resource unit | Cost screen and the Overview HUD |
| P4 | Artifact browser routes (list, detail with provenance) | Artifacts screen (8) |
| P5 | Workflow Run pause and resume routes | Visible "held gate" control; the runtime already supports it |
| P6 | Event list route with type filter and paging | Events screen beyond the live tail |
| P7 | `goal` / `projectId` (and `workflowRunId`) on `AgentCardData` (`GET /agents/active`) | Overview floor selector. Today it needs a join from each run through `AgentDetail.runs[].goal` to `GET /goals`, with one detail request per active agent. |
| P8 | `activeStop` on `AgentCardData`, or a batched agent-detail read | Overview stop barriers without N `GET /agents/:id` calls |
| P9 | `latestInvocation.kind` on `AgentCardData` | A station per Invocation kind in Overview rooms (D6). Also needs the Tile Pack station sheets sliced, with a slice map recorded. |
| P10 | A person-authored slice map for `Environment/Tilesets/{Floors,Wall,Dungeon}_Tiles.png` | Tile-built room floors, walls, doors and rune sigils. Rooms stay CSS chrome until it exists. **Approved** by the operator on 2026-09-14, as proposed: 34 cuts and 5 exclusions, read back from the Figma page "Tile cut map (review)" into `tile-slices.json` in this skill. The `.aseprite` sources contain no author slices or tilesets. |

P7–P10 were raised by the skill's GREEN verification run (an Overview build under the skill), 2026-09-14.
