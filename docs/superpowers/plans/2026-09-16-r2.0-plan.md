# R2.0 — Autonomous Work, Real-World Capabilities & Living AI Workplace

**Status:** draft. Stage 0 is under way (it needs no decision); the stages below wait on §4.
**Brief:** the operator's R2.0 brief, 2026-09-16. **Predecessor:** `docs/development/V1_1_CLOSURE.md`.
**Rule carried from V1.1:** no push; local commits; no new infrastructure without evidence; nothing bypasses governance; the Claude Max subscription adapter is the only production runtime and no API key is required.

## 1. What this milestone is

V1.1 proved the runtime can *govern* autonomous work. R2.0 has to prove the work is *useful*: real external evidence, affordable token economics, and agents that persist as identities in the keep. Everything below traces to one of two live demonstrations, or it is deferred:

- **Dogfood A — useful governed work.** One mission uses a genuine external capability, records provenance for every piece of external evidence, distinguishes it from model knowledge, and finishes on the agent's own decision rather than on a limit.
- **Dogfood B — a member of the workplace.** An agent is recruited with a chosen appearance, does real work, earns progression from that work, shows it on its profile, and returns to ambient life in the hub without generating a single fake runtime fact.

If either is not demonstrated, the closing report says so.

## 2. Evidence gathered before planning

Five independent read-only investigations (§55 of the brief), all reported: token economics, research providers, the art inventory, the progression data model, and the Stage 0 evidence gaps. Their findings are what §3–§7 rest on; where a number below is an estimate, it says so.

## 3. Stage 0 — V1.1 evidence closure (no decision needed)

| Gap | Verdict | Evidence |
|---|---|---|
| Long Markdown incl. fenced code | **Closed deterministically.** New test renders a >2,000-char body with a fenced block, a 9-column table and nested lists; the size also proves the Document view reads full content, not the API's preview. | `web/tests/deliverable.test.tsx`, commit `6b1e42b` |
| Versions line | **Closed live (no tokens).** Created a genuine Strategist v2 through `POST /agent-definitions`; the board shows `versions: v1 v2`, each linking to the other, at 1280 and 1920, with no horizontal scroll. | browser check, 2026-09-16 |
| Revoke closes pending approvals | **Already covered** at API level — the closure document understated it. | `tests/api/registryRoutes.test.ts:118` |
| Agent-decided finish + skip | **Covered in code**, including the negative assertions (positions never created, exact call counts). Live only adds proof that a *model* chooses to finish. | `tests/api/autonomousLoop.test.ts:243-293`, `tests/execution/skipPositions.test.ts:93-177` |
| `research.retrieve` "live" | **Category error.** The bound binding is the synthetic fixture by decision, and the test already runs the real adapter unmocked. Superseded by Stage 2. | `tests/api/autonomousLoop.test.ts:245` |
| Keeper Think | Format already validated live in V1.1; the route is covered. Low value. | `tests/api/keeper.test.ts:161-215` |
| Stop on an active run | Engine proven, including refusing to resolve a deferred spec while stopped. **But the UI has only an agent-scope stop:** halting one run halts every run of that agent, though the engine and API support run and workflow-run scope. | `tests/governance/executionStop.test.ts:404`; `web/components/StopControl.tsx:86-101` |
| Wide tables | The CSS is the standard `display:block; overflow-x:auto`, which jsdom cannot judge. Folded into the R2.0 browser QA, where research deliverables will contain real tables. | `deliverable.module.css:128-132` |

**Carried into R2.0 as work:** a run-scope stop control (a real gap, not a test gap), and the live agent-decided finish, which needs tokens (§4, D6).

## 4. Decisions required

Batched so they can be answered once. Nothing here is assumed in my favour; where a value is missing I will implement the mechanism and leave the value as a named constant.

| # | Decision | Why it needs you | My recommendation |
|---|---|---|---|
| **D1** | Scope of real research | A genuine credential-free capability exists, but **as of 2026-09-16 no free general web search does**. Wikipedia, Crossref, arXiv, OpenAlex and PubMed all answer with no key, no account and no card, under published terms. Every general web search option now needs an account, and most need a card: Brave withdrew its free tier in Feb 2026, Bing's API is retired, Google CSE is closed to new customers, and DuckDuckGo publishes no results API. Tavily has a real free tier but still needs an account and an API key, which the platform has no mechanism to hold. | **Build encyclopedic + scholarly search now, with no credentials**, and label it exactly as that — never as "web search". Revisit general search only if the missions demand it. |
| **D2** | XP weights and the level curve | The roadmap already lists XP rules as an open governance decision. I will not invent the numbers. | Three weights by task difficulty tag, plus a curve; I will propose values and implement them as named constants you can change. |
| **D3** | A human quality verdict event | Your §14 wants "validated positive outcomes" to matter, but the log contains no human judgment of output: approval is *pre-action authorization*, and rejection is the only negative. Grading needs a new event emitted by an operator route. | Add it, human-actor only, never emittable by an agent. Without it, XP can only reward completion, not quality. |
| **D4** | Peer endorsements | Nothing in the runtime is a peer verdict today, so this needs a new capability and event. A model-written endorsement is a claim, not a fact. | Build it with a deterministic anti-fabrication rule: the endorsing run must provably have had the endorsed artifact's id *and hash* in its compiled context, cross-lineage only, and worth zero XP on its own. |
| **D5** | Widening the sprite rule | The UI skill restricts characters to Knight, Rogue, Wizzard and Body_A, and the Rogue is already the Keeper. That leaves roughly two usable identities. The same rule says mobs carry no meaning — but the orc and skeleton crews are eight more silhouettes at exactly the right cell size and palette. | Widen to recoloured variants of the existing figures, and record it. I do **not** recommend pressing orcs and skeletons into service as staff: they read as enemies. If you want more silhouettes than recolouring gives, the honest answer is drawing new ones. |
| **D6** | Live-token envelope for R2 | V1.1 authorized exactly one live check and one dogfood. R2 needs more: the agent-decided finish (CHEAP, ~15k), the economics before/after pair on one objective (~90k), Dogfood A's research mission (~50k) and Dogfood B's recruit-and-work run (~50k), plus small format checks. | **~250,000 subscription tokens for all R2 live work**, with consumption reported per run. If you would rather approve run by run, say so and I will stop before each. |
| **D7** | Cost class for a free external call | The cost classes are `local_retrieval` and `metered_api`. A free but rate-limited third-party call is honestly neither. | Add a class rather than mislabel it. |
| **D8** | Operator contact string | Wikimedia's policy requires a client name and contact in the user agent; Crossref and OpenAlex want a `mailto`. It is configuration, not a credential, but it publishes an address to third parties. | You choose the string. |
| **D9** | HTML extraction | There is no HTML parser in the project. | A bounded tag-strip for the proof; a dependency only if extraction quality demands it. |
| **D10** | Where a chosen appearance lives | An Agent Definition accepts only name, role, objective, instructions, execution profile and grants, and definitions are immutable. Worse, today's look is derived from the **version's** id, so Strategist v2 already renders as a different character from v1 — one member of the workplace, two faces. | **Store appearance in a presentation table keyed on the agent's name**, which is already the cross-version identity. Restyling then changes no Definition and forks no history, and a new version inherits the face. It stays inert by construction: no policy, budget, router or context path reads it. |
| **D12** | Merging the loop's decide and act for thinking actions | The second-largest economy (~20–30% of a run) removes one model call per thinking iteration. But it changes the fixed plan shape you approved as R1 — the invariant that a plan's positions are allocated up front and can only skip. | Worth doing, but it is a change to an approved architecture decision, so it needs your second yes. Without it I take the other five reductions, which do not touch the plan shape. |
| **D11** | Idle agents walking vs the recorded motion rule | The design record says plainly: *animate what is working; idle is subdued*. Your brief wants idle agents to walk, sit, rest and gather. These contradict, and the art to do it does not exist yet (§7.4). | Record a deliberate reversal for the hub only — ambient life in shared space, stillness everywhere work is shown — rather than letting the rule erode quietly. |

**Stop conditions honoured:** no paid provider, no account, no credential and no spend is part of the recommended path.

### 4a. Answered by the operator, 2026-09-16

All twelve are decided; none may be silently reopened.

| # | Answer |
|---|---|
| D1 | **Yes** to credential-free encyclopedic and scholarly research (Wikipedia, Crossref, arXiv, OpenAlex, PubMed), labelled exactly that. **First**, investigate whether Claude's own native web search runs on the Max subscription runtime — if it does, it is the preferred path for general web research. No paid or account-bound search provider without explicit authorization. |
| D2 | **Yes.** XP: task 100, workflow 250, mission 500, validated artifact 150, research task 100, capability use 50, difficulty bonus 100–500. Verdicts: poor 0, acceptable 50, good 150, excellent 300. Levels: 0 / 500 / 1,250 / 2,250 / 3,500 / 5,000, rising after. Governance constants, easy to revise. No XP for model calls, tokens, approvals, idle or ambient activity, endorsements, or failed work. Difficulty must be derived from governed criteria, never self-declared. |
| D3 | **Yes.** Operator-only quality verdict (poor / acceptable / good / excellent), separate from approval, carrying operator provenance, the artifact/task/run it judges, a timestamp and an optional rationale. Never inferred from approval or rejection. |
| D4 | **Yes**, as a governed capability and event, requiring proof the endorser had the artifact and hash in its compiled context. Cross-lineage only. **Zero XP.** No self-endorsement, same-lineage farming or loops. |
| D5 | **No** to orcs and skeletons as staff. **New pixel-art agent assets are authorized** where existing folders lack something suitable, matching the established language, scale, perspective, palette and animation conventions. Originals preserved; significant work documented. |
| D6 | **250,000 subscription tokens** for all R2 live work, reported per run with cumulative use and remaining envelope. Hard limit: at exhaustion, stop — no extension, no API billing, no other paid provider. |
| D7 | **Yes**, a distinct free-external/retrieval class. Free is not unlimited: rate limits, availability and provider terms still govern. `usd`, `subscription_tokens` and `local_tokens` stay separate and are never combined. |
| D8 | **Yes.** No personal contact details: a configurable application identity ("Command Centre research client") plus a project URL where useful. |
| D9 | **No parser for now.** Structured JSON/XML interfaces only, until a demonstrated need. |
| D10 | **Yes.** Appearance belongs to the persistent agent identity, not the Definition version: v1 → v2 keeps the same character unless the operator deliberately changes it. |
| D11 | **Yes.** The idle-subdued rule is deliberately reversed for the Common Hub. Ambient life is presentation only and creates no task, run, invocation, artifact, event, token, XP, rating or performance change. Runtime state always overrides it. |
| D12 | **No for now.** Decide and act stay split; the approved R1 plan shape holds. Take the other reductions, measure, and only revisit with evidence. |

## 5. Stages

**MUST (the two dogfoods depend on them)**
1. Stage 1 — measure the loop's economics, then reduce tokens per useful outcome without touching a ceiling (§6).
2. Stage 2 — **built 2026-09-16.** Two Capabilities, not one, because their costs differ by three orders of magnitude:
   - **`research.search`** reads Wikipedia, Crossref and arXiv over HTTP. Credential-free, no account, no spend; proven live against all three with zero tokens. Each result carries source id, canonical URL, date, authors, container, a bounded snippet and its retrieval time, and its evidence class is `external`, so a deliverable may finally say real sources were consulted. It is labelled encyclopedic and scholarly — never "web search".
   - **`research.web`** searches the live web inside a governed model call, carrying only the tool its Grant authorized. It has no Tool Binding, because there is no tool to run.
   - **Deliberately not built:** OpenAlex (anonymous quota is ~100 searches a day, and lifting it needs a signup) and PubMed (its abstracts need a real XML parser — a dependency D9 has not approved). Both recorded as missing rather than half-built.
   - **Proven live, three times.** A governed autonomous Run searched arXiv and Crossref, analysed the results, searched the live web, and wrote a deliverable citing six real sources — every action ALLOWed by Policy against a real Grant, at propose and again pre-dispatch. A second run proved the search itself is recorded: four queries and seven source URLs on the Invocation that made them. The live runs found three defects the mocked suite could not: the evidence basis ignored capabilities that run inside a model call, the loop refused its own search over a neighbouring action's input field, and tool activity was written as an all-zero record on calls that used no tools.
   - **Known limit, from the same runs:** live web search returns titles and URLs, not page contents, so it answers "which sources discuss this" well and "what exactly is the current value" badly — it reported an npm version one release stale. A deliverable resting on it means *these sources were consulted*, not *this figure is verified*. And it is expensive: one CHEAP search cost ~35,000 tokens, two thirds of a Task Instance ceiling.
   - **`research.open` is not built.** The scholarly sources return their abstracts *in the search result*, so opening a document separately would fetch arbitrary web pages — which needs the SSRF guard and the HTML parser D9 declined. Deferred with its reason, rather than shipped as a fetcher that fails on a third of the web (npmjs.com already answers 403 to our client while the provider's own search reads it fine).
3. Stage 3 — one of those capabilities proven end to end through grant, policy, budget, approval, artifact and provenance, with fetched content treated as untrusted input.
4. Agent visual identity, chosen at recruitment, stored apart from anything conferring authority.
5. Progression — XP, levels, explainable metrics, specialisations, achievements — as a **sibling projection keyed on the agent's name**, which is already the cross-version identity, so no migration and no rewritten history.
6. Ambient hub life, client-side only, with real run state always overriding it.

**SHOULD**
7. Stage 4 — evidence-driven missions: gather, judge sufficiency, finish deliberately. **Built and proven live 2026-09-16.** A finish now cites the results it claims satisfy its completion criteria; code verifies each citation against the Run's own ledger (a completed tool action whose stored result holds results). Only verified evidence makes completion `evidence_sufficient`; an unsupported finish is recorded as `agent_finished` with the claim and rejected citations kept; a limit is always `incomplete` and never carries an evidence record; a policy stop or failure never reaches a deliverable. Live: run `13c60dc8` searched Wikipedia then Crossref and finished at iteration 3 of 4 on two verified citations, for 18,113 tokens. Scholarly/encyclopedic sources only, per the operator's Stage 4 decision.
8. Stage 6 — Keeper explanations for evidence, cost, stop reasons, ambient-vs-real, and why an agent has its level and reputation.
9. The run-scope stop control Stage 0 exposed.

**DEFER (named, not silently dropped)**
- Stage 5 multi-agent comparison — only once single-agent work is economically useful; the experiment is worth running, the infrastructure is not worth building before the result.
- General web search, any paid provider, any credential mechanism (D1).
- Event-driven triggers, long-running missions, persistent memory, vector stores, simulation/replay, sandboxing — all §38–44 futures, none evidenced yet.
- Reputation as a single score: refused on principle; multiple signals with their own sample counts instead.

## 6. Stage 1 — where the tokens actually went

Measured from the V1.1 dogfood's own recorded events (run `d84737a8`, 42,076 subscription tokens, six model calls), not estimated:

| Term | Share | Detail |
|---|---|---|
| Model **output** | **57%** (24,053) | Two thinking acts alone produced 6,527 and 9,042 output tokens. The act directive asks for "the full result as Markdown" with no length bound, and no output cap is passed to the CLI. |
| Compiled **context** | ~30% | Reported primary input is 2–4 tokens per call: the prompt lands in cache-creation tokens, which are not counted. It is then re-paid almost exactly once by the secondary Haiku call. |
| Fixed per-call overhead | ~13% | ≈ 890 tokens per call, whatever the call does. |

So the working cost model is **counted ≈ context + ~890 + output**, and the "6k per call" figure in the V1.1 record is a run average hiding a 2,000–12,674 spread. Two concrete wastes in that one run: 4,982 tokens on the decide whose invented action was refused (already fixed in `26f29db`), and 7,924 tokens stranded because the headroom check reserves 21,600 for an iteration that actually costs ~13,000.

**Planned reductions, in order, none of which touches a ceiling or a governance check:**
1. **Bound intermediate output.** Cap what a thinking act writes (it only feeds the ledger and the final write) more tightly than the deliverable itself. Largest single lever, est. 17–26% of a run. **Verified live on 2026-09-16** (one minimal call, ~1k tokens): the runtime has no output-cap flag, but it *does* honour a schema `maxLength` — asked for 300 words under a 40-character cap, it returned 37 characters. So this needs no new mechanism and no decision: it is a bound in the output schema the loop already sends.
2. **Merge decide and act for thinking actions only** — one call instead of two, saving its overhead, its re-paid context and its decision output (est. 20–30%). Tool and gate actions must stay split, because the Approval pins the immutable decision artifact.
3. **Honour a deterministic finish** — `done: true` without an explicit finish action currently does not stop the loop, wasting a whole iteration when it happens.
4. **Trim the decision's own output** — `assessment` is stored and never read by any code.
5. **Compact the ledger and refer to prior results by id.** This also removes a latent cliff: at around iteration 8–9 the ledger crosses the decide budget and is silently excluded from context entirely.
6. **Re-size the loop's reservations** — but only after output is bounded, because today's estimates already *under*-shoot actual acts.

**Done in this phase (2026-09-16):** reductions 1, 3 and 4 are built — every model-written field now carries a length bound and every list a size bound (`OUTPUT_CAPS`, `DELIVERABLE_CAPS`), and a decision that says the objective is met now ends the loop instead of buying another iteration. That second one exposed a real defect: the loop stopped for the right reason and then *reported* the wrong one, because the terminal reason is re-derived at conclude rather than recorded when the stop happens. Both the stop and the conclusion now use the same rule. Reductions 2 (merge decide and act) is refused by D12; 5 (ledger compaction) and 6 (re-sized reservations) remain.

**Measurement harness:** a read-only query over existing events (`invocation_completed` usage, `context_compiled` sizes and exclusions, `agent_loop_iteration_recorded` outcomes) plus a mocked regression asserting call counts and shapes, so no future refactor can quietly add a round-trip. Two gaps to close first: per-layer attribution (instructions and the invocation instruction are never itemised) and the secondary model's usage (recoverable by arithmetic today, unrecoverable at CHEAP).

**Honest caveat:** all of this optimises the *counted* metric. Whether cache-creation tokens draw on real Max entitlement is a documented unknown; if they do, context becomes far more expensive than the counted numbers suggest and reduction 5 rises sharply in value.

## 7. Architecture positions (no second anything)

1. **No new permission system.** Research and every external capability run through Capability → Grant → Policy → Budget → Approval, as data plus a registered adapter.
2. **Progression never touches authority.** It gets no eligibility gate, is not imported by governance, router or execution, and — importantly — never enters a compiled context, so an agent cannot optimise for its own XP.
3. **Lineage is the agent's name**, already structural via the unique `(name, version)` index. The spec's per-version XP key contradicts persistence across versions and needs correcting.
4. **Pixel art: the ambition needs new art, and two decisions first.** The inventory is blunt. The characters that match the keep's scale are Knight, Rogue and Wizzard (plus a nude base body): the Rogue is already the Keeper, and the Wizzard's purple collides with three identity tints, so **about two identities exist today and roughly seven or eight are reachable by recolouring**. Every sprite is flat and fully baked — there is no hair, clothing or equipment layer in any pack — so the Builder can offer **presets plus a palette, not mix-and-match**, unless layered art is drawn. There is **no walk animation, one facing, and no sit or rest pose** for any of these characters, and no walkable-area or seat data of any kind. So an idle agent that walks to a rest room needs generated art: walk strips with at least two facings, sit/rest poses, a rest room in a recomposed keep, and a waypoint map. All of it must follow the existing pipeline (read-only over the source packs, scripts and output under `assets/gamification/adapted/`, whole-number scaling, baked outline, identity tints only — never state colours).
5. **Ambient life is presentation.** No backend, no game engine, no network traffic from movement, no ambient state that the runtime does not actually produce.
6. **SSRF is the real work in `research.open`**: refuse loopback, private, link-local and cloud-metadata addresses after DNS resolution and on every redirect hop; cap bytes, redirects and time.

## 8. Noticed in passing (not fixed, not mine to fix)

- **The art pipeline slices death animations mid-frame.** The strip script cuts every death sheet at 32px, but the source files say Knight death is 6 frames of 48×32 and Wizzard and Rogue 6 of 64×32 — which is why the frame counts carried in code are 9 and 12 instead of 6. Cosmetic today; it would matter the moment a death pose is used.
- **The only readable licence in the repo is currently deleted in the working tree.** `Terms.txt` for the Pixel Crawler pack shows as deleted in another workstream's uncommitted changes; it survives in git history, and it is the one pack whose terms can still be read (it permits commercial use and alteration, but forbids selling the assets as a product). The other four packs have **no licence file on disk at all** — their permissions rest on earlier recorded decisions rather than any document. I have touched none of these files.
- **Progression needs one prerequisite refactor**: the rule deciding which runs count is currently locked inside the performance projection by a structural firewall, so it must move to a neutral module both projections can read — otherwise the two would silently drift on what counts as a run.

## 9. Definition of done

The brief's 33 conditions map onto Dogfood A (1–12, 32–33), Dogfood B (13–31), and the measurement in §6 for condition 6. The closing report follows §57 and ends with PUSHED: NO.
